<form method="post" action="{{ path('app.admin.users.delete', {'id': administration_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ administration_user.id) }}">
    <button class="btn">Delete</button>
</form>
